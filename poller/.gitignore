# Secrets and machine-local state. VendoMonitor is a PUBLIC repo -- a secret
# committed here is burned even if a later commit removes it, because it stays
# in history. Do not remove these rules.
.env
*.last_good.py
mikrotik_status_poller.py.bak
spawn_poller.log
spawn_poller.log.1
__pycache__/
